const express = require("express"); 

//Creating rest object by whose help we can create the server
const app = express();


// routes
app.get("/test" ,)


//port

const PORT = 8080;

//LISTEN function
app.listen(PORT, () => {
    app.listen(PORT, () => {
        console.log(
          "Node Server Running"
        );
      })
  })